import React from 'react';
import renderer from 'react-test-renderer';
import SettingsScreen from '../SettingsScreen';

describe('Settings Screen', () => {
    // const NavigatorMock = renderer.create()
    // const SettingsScreenMock = renderer.create(<SettingsScreen route={ } navigation={ } />)

    it('renders correctly', () => {
        // snapshot test
    })
})

describe('Back Button', () => {
    it('takes the user to the previous training screen')

    it('updates the global settings context (state)')
})